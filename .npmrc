# Configuration npm pour le projet

# Auditer automatiquement à chaque install
audit=true
audit-level=moderate

# Enregistrer le package-lock exact
save-exact=false
save-prefix=^

# Ignorer les avertissements de moteur (à retirer après migration Node 20)
# engine-strict=false
